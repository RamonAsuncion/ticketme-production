defmodule Ticketme.Repo.Migrations.AddProfilePictureToUsers do
  use Ecto.Migration

  def change do
    # alter table(:users) do
    #   add :profile_picture, :string, null: false
    # end
  end
end
